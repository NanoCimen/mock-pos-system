PORT=3001
DATABASE_URL=postgresql://localhost:5432/mock_pos_db

# API Keys for authentication (comma-separated)
# Generate unique keys for each restaurant
API_KEYS=mock_pos_key_rest1,mock_pos_key_rest2,mock_pos_key_rest3
